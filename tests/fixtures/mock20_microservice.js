const overridePathPlannerCalls = () => {
  // No external path-planner calls are needed for this test.
};

const overrideMapServerCalls = () => {
  // No external map-service calls are needed for this test.
};

module.exports = {
  overridePathPlannerCalls,
  overrideMapServerCalls,
};
